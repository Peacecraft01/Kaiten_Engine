#ifndef ENUMS_H
#define ENUMS_H

enum ELEMENTS {
    FIRE,
    WATER,
    ICE,
    ELECTRIC,
    EARTH,
    LIGHT,
    DARK,
    NUM_ELEMENTS
};

enum TYPES {
    SLASH,
    PIERCE,
    BLUNT,
    ARMOR,
    SHIELD,
    NUM_TYPES
};

enum STATUSES {
    BURN,
    PARALYZE,
    BLEED,
    HEMERAGE,
    CRUSH,
    NUM_STATUSES
};

#endif